class Mundo {
constructor(m){this.m=m;this.chunks=new Map();this.tam=16;this.radioCarga=4;this.radioEliminar=5;}
altura(x,z){return ruido(x*.08,z*.08)*6+ruido(x*.25,z*.25)*1.2;}
tipoBloque(h){if(h<.5)return'agua';if(h<2)return'arena';if(h<4)return'cesped';if(h<6)return'tierra';return'piedra';}
crearArbol(x,z,h,chunk){const arbol=new THREE.Group();const tronco=new THREE.Mesh(new THREE.CylinderGeometry(.25,.35,2.5),new THREE.MeshLambertMaterial({color:0x92400e}));const hojas=new THREE.Mesh(new THREE.SphereGeometry(1.2),new THREE.MeshLambertMaterial({color:0x15803d}));tronco.position.y=1.25;hojas.position.y=3;arbol.add(tronco,hojas);arbol.position.set(x,h,z);arbol.tipo='arbol';arbol.userData.recurso={madera:3,palo:1};tronco.userData.recurso=arbol.userData.recurso;hojas.userData.recurso=arbol.userData.recurso;this.m.escena.add(arbol);chunk.objetos.push(arbol);}
crearRoca(x,z,h,chunk){const roca=new THREE.Mesh(new THREE.DodecahedronGeometry(.7),new THREE.MeshLambertMaterial({color:0x6b7280}));roca.position.set(x,h+.5,z);roca.tipo='roca';roca.userData.recurso={piedra:3};this.m.escena.add(roca);chunk.objetos.push(roca);}
crearAnimal(x,z,tipo,chunk){const color=tipo==='lobo'?0x555555:0x92400e;const animal=new THREE.Mesh(new THREE.BoxGeometry(1,1,1.8),new THREE.MeshLambertMaterial({color}));animal.position.set(x,1,z);animal.tipo=tipo;this.m.escena.add(animal);chunk.objetos.push(animal);if(this.m.criaturas)this.m.criaturas.lista.push({tipo,x,z,y:1,vida:100,daño:10,modelo:animal});}
generar(cx,cz){const clave=`${cx},${cz}`;if(this.chunks.has(clave))return;const chunk={grupo:new THREE.Group(),objetos:[]};const geo=new THREE.BoxGeometry();for(let x=0;x<this.tam;x++){for(let z=0;z<this.tam;z++){const wx=cx*this.tam+x,wz=cz*this.tam+z,h=Math.floor(this.altura(wx,wz)),tipo=this.tipoBloque(h),colores={agua:0x1d4ed8,arena:0xfcd34d,cesped:0x22c55e,tierra:0x92400e,piedra:0x6b7280};const bloque=new THREE.Mesh(geo,new THREE.MeshLambertMaterial({color:colores[tipo]}));bloque.position.set(wx,h/2,wz);bloque.scale.y=Math.max(.3,h);chunk.grupo.add(bloque);if(tipo==='cesped'&&Math.random()<.05)this.crearArbol(wx,wz,h,chunk);if(tipo==='piedra'&&Math.random()<.04)this.crearRoca(wx,wz,h,chunk);}}
this.chunks.set(clave,chunk);this.m.escena.add(chunk.grupo);}
eliminarLejanos(cx,cz){for(const [clave,chunk] of this.chunks){const p=clave.split(','),x=parseInt(p[0]),z=parseInt(p[1]),d=Math.max(Math.abs(cx-x),Math.abs(cz-z));if(d>this.radioEliminar){chunk.objetos.forEach(o=>this.m.escena.remove(o));this.m.escena.remove(chunk.grupo);this.chunks.delete(clave);}}}
actualizar(jx,jz){const cx=Math.floor(jx/this.tam),cz=Math.floor(jz/this.tam);for(let x=-this.radioCarga;x<=this.radioCarga;x++)for(let z=-this.radioCarga;z<=this.radioCarga;z++)this.generar(cx+x,cz+z);this.eliminarLejanos(cx,cz);}
}
window.Mundo=Mundo;

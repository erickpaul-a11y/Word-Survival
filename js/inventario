class Inventario {
    constructor() {
        this.items = [];
    }

    agregar(tipo, cantidad=1) {
        const existe = this.items.find(i => i.tipo === tipo);
        if (existe) existe.cantidad += cantidad;
        else this.items.push({ tipo, cantidad });
    }
}
